/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getArrayDecoder,
  getArrayEncoder,
  getBooleanDecoder,
  getBooleanEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type EncodedAccount,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const ORACLE_POOL_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  42, 185, 120, 24, 182, 8, 43, 146,
]);

export function getOraclePoolDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(ORACLE_POOL_DISCRIMINATOR);
}

export type OraclePool = {
  discriminator: ReadonlyUint8Array;
  bump: number;
  adminPool: Address;
  tokenMint: Address;
  feedId: ReadonlyUint8Array;
  isApproved: boolean;
  padding: Array<bigint>;
  /** Trailing reserve that pads `OraclePool` to its fixed 208-byte on-chain allocation. */
  reserved: ReadonlyUint8Array;
};

export type OraclePoolArgs = {
  bump: number;
  adminPool: Address;
  tokenMint: Address;
  feedId: ReadonlyUint8Array;
  isApproved: boolean;
  padding: Array<number | bigint>;
  /** Trailing reserve that pads `OraclePool` to its fixed 208-byte on-chain allocation. */
  reserved: ReadonlyUint8Array;
};

/** Gets the encoder for {@link OraclePoolArgs} account data. */
export function getOraclePoolEncoder(): FixedSizeEncoder<OraclePoolArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["bump", getU8Encoder()],
      ["adminPool", getAddressEncoder()],
      ["tokenMint", getAddressEncoder()],
      ["feedId", fixEncoderSize(getBytesEncoder(), 66)],
      ["isApproved", getBooleanEncoder()],
      ["padding", getArrayEncoder(getU64Encoder(), { size: 8 })],
      ["reserved", fixEncoderSize(getBytesEncoder(), 4)],
    ]),
    (value) => ({ ...value, discriminator: ORACLE_POOL_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link OraclePool} account data. */
export function getOraclePoolDecoder(): FixedSizeDecoder<OraclePool> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["bump", getU8Decoder()],
    ["adminPool", getAddressDecoder()],
    ["tokenMint", getAddressDecoder()],
    ["feedId", fixDecoderSize(getBytesDecoder(), 66)],
    ["isApproved", getBooleanDecoder()],
    ["padding", getArrayDecoder(getU64Decoder(), { size: 8 })],
    ["reserved", fixDecoderSize(getBytesDecoder(), 4)],
  ]);
}

/** Gets the codec for {@link OraclePool} account data. */
export function getOraclePoolCodec(): FixedSizeCodec<
  OraclePoolArgs,
  OraclePool
> {
  return combineCodec(getOraclePoolEncoder(), getOraclePoolDecoder());
}

export function decodeOraclePool<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<OraclePool, TAddress>;
export function decodeOraclePool<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<OraclePool, TAddress>;
export function decodeOraclePool<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<OraclePool, TAddress> | MaybeAccount<OraclePool, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getOraclePoolDecoder(),
  );
}

export async function fetchOraclePool<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<OraclePool, TAddress>> {
  const maybeAccount = await fetchMaybeOraclePool(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeOraclePool<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<OraclePool, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeOraclePool(maybeAccount);
}

export async function fetchAllOraclePool(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<OraclePool>[]> {
  const maybeAccounts = await fetchAllMaybeOraclePool(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeOraclePool(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<OraclePool>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeOraclePool(maybeAccount));
}

export function getOraclePoolSize(): number {
  return 208;
}
