/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findAdminPoolPda, findOraclePoolPda } from "../pdas";
import { FBYT_VAULT_PROGRAM_ADDRESS } from "../programs";

export const UPDATE_ORACLE_POOL_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([219, 158, 32, 167, 160, 27, 134, 167]);

export function getUpdateOraclePoolDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    UPDATE_ORACLE_POOL_DISCRIMINATOR,
  );
}

export type UpdateOraclePoolInstruction<
  TProgram extends string = typeof FBYT_VAULT_PROGRAM_ADDRESS,
  TAccountAdmin extends string | AccountMeta<string> = string,
  TAccountAdminPool extends string | AccountMeta<string> = string,
  TAccountTokenMint extends string | AccountMeta<string> = string,
  TAccountOraclePool extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAdmin extends string
        ? WritableSignerAccount<TAccountAdmin> &
            AccountSignerMeta<TAccountAdmin>
        : TAccountAdmin,
      TAccountAdminPool extends string
        ? WritableAccount<TAccountAdminPool>
        : TAccountAdminPool,
      TAccountTokenMint extends string
        ? WritableAccount<TAccountTokenMint>
        : TAccountTokenMint,
      TAccountOraclePool extends string
        ? WritableAccount<TAccountOraclePool>
        : TAccountOraclePool,
      ...TRemainingAccounts,
    ]
  >;

export type UpdateOraclePoolInstructionData = {
  discriminator: ReadonlyUint8Array;
  feedId: string;
};

export type UpdateOraclePoolInstructionDataArgs = { feedId: string };

export function getUpdateOraclePoolInstructionDataEncoder(): Encoder<UpdateOraclePoolInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["feedId", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
    ]),
    (value) => ({ ...value, discriminator: UPDATE_ORACLE_POOL_DISCRIMINATOR }),
  );
}

export function getUpdateOraclePoolInstructionDataDecoder(): Decoder<UpdateOraclePoolInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["feedId", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
  ]);
}

export function getUpdateOraclePoolInstructionDataCodec(): Codec<
  UpdateOraclePoolInstructionDataArgs,
  UpdateOraclePoolInstructionData
> {
  return combineCodec(
    getUpdateOraclePoolInstructionDataEncoder(),
    getUpdateOraclePoolInstructionDataDecoder(),
  );
}

export type UpdateOraclePoolAsyncInput<
  TAccountAdmin extends string = string,
  TAccountAdminPool extends string = string,
  TAccountTokenMint extends string = string,
  TAccountOraclePool extends string = string,
> = {
  admin: TransactionSigner<TAccountAdmin>;
  adminPool?: Address<TAccountAdminPool>;
  tokenMint: Address<TAccountTokenMint>;
  oraclePool?: Address<TAccountOraclePool>;
  feedId: UpdateOraclePoolInstructionDataArgs["feedId"];
};

export async function getUpdateOraclePoolInstructionAsync<
  TAccountAdmin extends string,
  TAccountAdminPool extends string,
  TAccountTokenMint extends string,
  TAccountOraclePool extends string,
  TProgramAddress extends Address = typeof FBYT_VAULT_PROGRAM_ADDRESS,
>(
  input: UpdateOraclePoolAsyncInput<
    TAccountAdmin,
    TAccountAdminPool,
    TAccountTokenMint,
    TAccountOraclePool
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  UpdateOraclePoolInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountAdminPool,
    TAccountTokenMint,
    TAccountOraclePool
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? FBYT_VAULT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    admin: { value: input.admin ?? null, isWritable: true },
    adminPool: { value: input.adminPool ?? null, isWritable: true },
    tokenMint: { value: input.tokenMint ?? null, isWritable: true },
    oraclePool: { value: input.oraclePool ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.adminPool.value) {
    accounts.adminPool.value = await findAdminPoolPda({ programAddress });
  }
  if (!accounts.oraclePool.value) {
    accounts.oraclePool.value = await findOraclePoolPda(
      {
        adminPool: getAddressFromResolvedInstructionAccount(
          "adminPool",
          accounts.adminPool.value,
        ),
        tokenMint: getAddressFromResolvedInstructionAccount(
          "tokenMint",
          accounts.tokenMint.value,
        ),
      },
      { programAddress },
    );
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("admin", accounts.admin),
      getAccountMeta("adminPool", accounts.adminPool),
      getAccountMeta("tokenMint", accounts.tokenMint),
      getAccountMeta("oraclePool", accounts.oraclePool),
    ],
    data: getUpdateOraclePoolInstructionDataEncoder().encode(
      args as UpdateOraclePoolInstructionDataArgs,
    ),
    programAddress,
  } as UpdateOraclePoolInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountAdminPool,
    TAccountTokenMint,
    TAccountOraclePool
  >);
}

export type UpdateOraclePoolInput<
  TAccountAdmin extends string = string,
  TAccountAdminPool extends string = string,
  TAccountTokenMint extends string = string,
  TAccountOraclePool extends string = string,
> = {
  admin: TransactionSigner<TAccountAdmin>;
  adminPool: Address<TAccountAdminPool>;
  tokenMint: Address<TAccountTokenMint>;
  oraclePool: Address<TAccountOraclePool>;
  feedId: UpdateOraclePoolInstructionDataArgs["feedId"];
};

export function getUpdateOraclePoolInstruction<
  TAccountAdmin extends string,
  TAccountAdminPool extends string,
  TAccountTokenMint extends string,
  TAccountOraclePool extends string,
  TProgramAddress extends Address = typeof FBYT_VAULT_PROGRAM_ADDRESS,
>(
  input: UpdateOraclePoolInput<
    TAccountAdmin,
    TAccountAdminPool,
    TAccountTokenMint,
    TAccountOraclePool
  >,
  config?: { programAddress?: TProgramAddress },
): UpdateOraclePoolInstruction<
  TProgramAddress,
  TAccountAdmin,
  TAccountAdminPool,
  TAccountTokenMint,
  TAccountOraclePool
> {
  // Program address.
  const programAddress = config?.programAddress ?? FBYT_VAULT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    admin: { value: input.admin ?? null, isWritable: true },
    adminPool: { value: input.adminPool ?? null, isWritable: true },
    tokenMint: { value: input.tokenMint ?? null, isWritable: true },
    oraclePool: { value: input.oraclePool ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("admin", accounts.admin),
      getAccountMeta("adminPool", accounts.adminPool),
      getAccountMeta("tokenMint", accounts.tokenMint),
      getAccountMeta("oraclePool", accounts.oraclePool),
    ],
    data: getUpdateOraclePoolInstructionDataEncoder().encode(
      args as UpdateOraclePoolInstructionDataArgs,
    ),
    programAddress,
  } as UpdateOraclePoolInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountAdminPool,
    TAccountTokenMint,
    TAccountOraclePool
  >);
}

export type ParsedUpdateOraclePoolInstruction<
  TProgram extends string = typeof FBYT_VAULT_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    admin: TAccountMetas[0];
    adminPool: TAccountMetas[1];
    tokenMint: TAccountMetas[2];
    oraclePool: TAccountMetas[3];
  };
  data: UpdateOraclePoolInstructionData;
};

export function parseUpdateOraclePoolInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedUpdateOraclePoolInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 4,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      admin: getNextAccount(),
      adminPool: getNextAccount(),
      tokenMint: getNextAccount(),
      oraclePool: getNextAccount(),
    },
    data: getUpdateOraclePoolInstructionDataDecoder().decode(instruction.data),
  };
}
