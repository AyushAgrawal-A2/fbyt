/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findAdminPoolPda } from "../pdas";
import { FBYT_VAULT_PROGRAM_ADDRESS } from "../programs";

export const GET_PRICE_INFO_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  214, 168, 85, 16, 228, 217, 38, 6,
]);

export function getGetPriceInfoDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    GET_PRICE_INFO_DISCRIMINATOR,
  );
}

export type GetPriceInfoInstruction<
  TProgram extends string = typeof FBYT_VAULT_PROGRAM_ADDRESS,
  TAccountPayer extends string | AccountMeta<string> = string,
  TAccountAdminPool extends string | AccountMeta<string> = string,
  TAccountPriceUpdate extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountPayer extends string
        ? WritableSignerAccount<TAccountPayer> &
            AccountSignerMeta<TAccountPayer>
        : TAccountPayer,
      TAccountAdminPool extends string
        ? ReadonlyAccount<TAccountAdminPool>
        : TAccountAdminPool,
      TAccountPriceUpdate extends string
        ? WritableAccount<TAccountPriceUpdate>
        : TAccountPriceUpdate,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type GetPriceInfoInstructionData = {
  discriminator: ReadonlyUint8Array;
  feedId: string;
};

export type GetPriceInfoInstructionDataArgs = { feedId: string };

export function getGetPriceInfoInstructionDataEncoder(): Encoder<GetPriceInfoInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["feedId", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
    ]),
    (value) => ({ ...value, discriminator: GET_PRICE_INFO_DISCRIMINATOR }),
  );
}

export function getGetPriceInfoInstructionDataDecoder(): Decoder<GetPriceInfoInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["feedId", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
  ]);
}

export function getGetPriceInfoInstructionDataCodec(): Codec<
  GetPriceInfoInstructionDataArgs,
  GetPriceInfoInstructionData
> {
  return combineCodec(
    getGetPriceInfoInstructionDataEncoder(),
    getGetPriceInfoInstructionDataDecoder(),
  );
}

export type GetPriceInfoAsyncInput<
  TAccountPayer extends string = string,
  TAccountAdminPool extends string = string,
  TAccountPriceUpdate extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  payer: TransactionSigner<TAccountPayer>;
  adminPool?: Address<TAccountAdminPool>;
  priceUpdate: Address<TAccountPriceUpdate>;
  systemProgram?: Address<TAccountSystemProgram>;
  feedId: GetPriceInfoInstructionDataArgs["feedId"];
};

export async function getGetPriceInfoInstructionAsync<
  TAccountPayer extends string,
  TAccountAdminPool extends string,
  TAccountPriceUpdate extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof FBYT_VAULT_PROGRAM_ADDRESS,
>(
  input: GetPriceInfoAsyncInput<
    TAccountPayer,
    TAccountAdminPool,
    TAccountPriceUpdate,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  GetPriceInfoInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountAdminPool,
    TAccountPriceUpdate,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? FBYT_VAULT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    payer: { value: input.payer ?? null, isWritable: true },
    adminPool: { value: input.adminPool ?? null, isWritable: false },
    priceUpdate: { value: input.priceUpdate ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.adminPool.value) {
    accounts.adminPool.value = await findAdminPoolPda({ programAddress });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("payer", accounts.payer),
      getAccountMeta("adminPool", accounts.adminPool),
      getAccountMeta("priceUpdate", accounts.priceUpdate),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getGetPriceInfoInstructionDataEncoder().encode(
      args as GetPriceInfoInstructionDataArgs,
    ),
    programAddress,
  } as GetPriceInfoInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountAdminPool,
    TAccountPriceUpdate,
    TAccountSystemProgram
  >);
}

export type GetPriceInfoInput<
  TAccountPayer extends string = string,
  TAccountAdminPool extends string = string,
  TAccountPriceUpdate extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  payer: TransactionSigner<TAccountPayer>;
  adminPool: Address<TAccountAdminPool>;
  priceUpdate: Address<TAccountPriceUpdate>;
  systemProgram?: Address<TAccountSystemProgram>;
  feedId: GetPriceInfoInstructionDataArgs["feedId"];
};

export function getGetPriceInfoInstruction<
  TAccountPayer extends string,
  TAccountAdminPool extends string,
  TAccountPriceUpdate extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof FBYT_VAULT_PROGRAM_ADDRESS,
>(
  input: GetPriceInfoInput<
    TAccountPayer,
    TAccountAdminPool,
    TAccountPriceUpdate,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): GetPriceInfoInstruction<
  TProgramAddress,
  TAccountPayer,
  TAccountAdminPool,
  TAccountPriceUpdate,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? FBYT_VAULT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    payer: { value: input.payer ?? null, isWritable: true },
    adminPool: { value: input.adminPool ?? null, isWritable: false },
    priceUpdate: { value: input.priceUpdate ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("payer", accounts.payer),
      getAccountMeta("adminPool", accounts.adminPool),
      getAccountMeta("priceUpdate", accounts.priceUpdate),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getGetPriceInfoInstructionDataEncoder().encode(
      args as GetPriceInfoInstructionDataArgs,
    ),
    programAddress,
  } as GetPriceInfoInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountAdminPool,
    TAccountPriceUpdate,
    TAccountSystemProgram
  >);
}

export type ParsedGetPriceInfoInstruction<
  TProgram extends string = typeof FBYT_VAULT_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    payer: TAccountMetas[0];
    adminPool: TAccountMetas[1];
    priceUpdate: TAccountMetas[2];
    systemProgram: TAccountMetas[3];
  };
  data: GetPriceInfoInstructionData;
};

export function parseGetPriceInfoInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedGetPriceInfoInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 4,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      payer: getNextAccount(),
      adminPool: getNextAccount(),
      priceUpdate: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getGetPriceInfoInstructionDataDecoder().decode(instruction.data),
  };
}
