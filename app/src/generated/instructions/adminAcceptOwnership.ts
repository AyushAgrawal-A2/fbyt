/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findAdminPoolPda } from "../pdas";
import { FBYT_VAULT_PROGRAM_ADDRESS } from "../programs";

export const ADMIN_ACCEPT_OWNERSHIP_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([102, 203, 241, 127, 141, 1, 31, 46]);

export function getAdminAcceptOwnershipDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    ADMIN_ACCEPT_OWNERSHIP_DISCRIMINATOR,
  );
}

export type AdminAcceptOwnershipInstruction<
  TProgram extends string = typeof FBYT_VAULT_PROGRAM_ADDRESS,
  TAccountPendingAdmin extends string | AccountMeta<string> = string,
  TAccountAdminPool extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountPendingAdmin extends string
        ? WritableSignerAccount<TAccountPendingAdmin> &
            AccountSignerMeta<TAccountPendingAdmin>
        : TAccountPendingAdmin,
      TAccountAdminPool extends string
        ? WritableAccount<TAccountAdminPool>
        : TAccountAdminPool,
      ...TRemainingAccounts,
    ]
  >;

export type AdminAcceptOwnershipInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type AdminAcceptOwnershipInstructionDataArgs = {};

export function getAdminAcceptOwnershipInstructionDataEncoder(): FixedSizeEncoder<AdminAcceptOwnershipInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({
      ...value,
      discriminator: ADMIN_ACCEPT_OWNERSHIP_DISCRIMINATOR,
    }),
  );
}

export function getAdminAcceptOwnershipInstructionDataDecoder(): FixedSizeDecoder<AdminAcceptOwnershipInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getAdminAcceptOwnershipInstructionDataCodec(): FixedSizeCodec<
  AdminAcceptOwnershipInstructionDataArgs,
  AdminAcceptOwnershipInstructionData
> {
  return combineCodec(
    getAdminAcceptOwnershipInstructionDataEncoder(),
    getAdminAcceptOwnershipInstructionDataDecoder(),
  );
}

export type AdminAcceptOwnershipAsyncInput<
  TAccountPendingAdmin extends string = string,
  TAccountAdminPool extends string = string,
> = {
  pendingAdmin: TransactionSigner<TAccountPendingAdmin>;
  adminPool?: Address<TAccountAdminPool>;
};

export async function getAdminAcceptOwnershipInstructionAsync<
  TAccountPendingAdmin extends string,
  TAccountAdminPool extends string,
  TProgramAddress extends Address = typeof FBYT_VAULT_PROGRAM_ADDRESS,
>(
  input: AdminAcceptOwnershipAsyncInput<
    TAccountPendingAdmin,
    TAccountAdminPool
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  AdminAcceptOwnershipInstruction<
    TProgramAddress,
    TAccountPendingAdmin,
    TAccountAdminPool
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? FBYT_VAULT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    pendingAdmin: { value: input.pendingAdmin ?? null, isWritable: true },
    adminPool: { value: input.adminPool ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.adminPool.value) {
    accounts.adminPool.value = await findAdminPoolPda({ programAddress });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("pendingAdmin", accounts.pendingAdmin),
      getAccountMeta("adminPool", accounts.adminPool),
    ],
    data: getAdminAcceptOwnershipInstructionDataEncoder().encode({}),
    programAddress,
  } as AdminAcceptOwnershipInstruction<
    TProgramAddress,
    TAccountPendingAdmin,
    TAccountAdminPool
  >);
}

export type AdminAcceptOwnershipInput<
  TAccountPendingAdmin extends string = string,
  TAccountAdminPool extends string = string,
> = {
  pendingAdmin: TransactionSigner<TAccountPendingAdmin>;
  adminPool: Address<TAccountAdminPool>;
};

export function getAdminAcceptOwnershipInstruction<
  TAccountPendingAdmin extends string,
  TAccountAdminPool extends string,
  TProgramAddress extends Address = typeof FBYT_VAULT_PROGRAM_ADDRESS,
>(
  input: AdminAcceptOwnershipInput<TAccountPendingAdmin, TAccountAdminPool>,
  config?: { programAddress?: TProgramAddress },
): AdminAcceptOwnershipInstruction<
  TProgramAddress,
  TAccountPendingAdmin,
  TAccountAdminPool
> {
  // Program address.
  const programAddress = config?.programAddress ?? FBYT_VAULT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    pendingAdmin: { value: input.pendingAdmin ?? null, isWritable: true },
    adminPool: { value: input.adminPool ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("pendingAdmin", accounts.pendingAdmin),
      getAccountMeta("adminPool", accounts.adminPool),
    ],
    data: getAdminAcceptOwnershipInstructionDataEncoder().encode({}),
    programAddress,
  } as AdminAcceptOwnershipInstruction<
    TProgramAddress,
    TAccountPendingAdmin,
    TAccountAdminPool
  >);
}

export type ParsedAdminAcceptOwnershipInstruction<
  TProgram extends string = typeof FBYT_VAULT_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    pendingAdmin: TAccountMetas[0];
    adminPool: TAccountMetas[1];
  };
  data: AdminAcceptOwnershipInstructionData;
};

export function parseAdminAcceptOwnershipInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedAdminAcceptOwnershipInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 2,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { pendingAdmin: getNextAccount(), adminPool: getNextAccount() },
    data: getAdminAcceptOwnershipInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
