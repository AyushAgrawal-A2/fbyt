/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findAdminPoolPda, findMoneyManagerPoolPda } from "../pdas";
import { FBYT_VAULT_PROGRAM_ADDRESS } from "../programs";

export const CREATE_MONEY_MANAGER_POOL_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([169, 250, 152, 215, 39, 206, 204, 11]);

export function getCreateMoneyManagerPoolDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CREATE_MONEY_MANAGER_POOL_DISCRIMINATOR,
  );
}

export type CreateMoneyManagerPoolInstruction<
  TProgram extends string = typeof FBYT_VAULT_PROGRAM_ADDRESS,
  TAccountAdminPool extends string | AccountMeta<string> = string,
  TAccountMoneyManager extends string | AccountMeta<string> = string,
  TAccountMoneyManagerPool extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAdminPool extends string
        ? ReadonlyAccount<TAccountAdminPool>
        : TAccountAdminPool,
      TAccountMoneyManager extends string
        ? WritableSignerAccount<TAccountMoneyManager> &
            AccountSignerMeta<TAccountMoneyManager>
        : TAccountMoneyManager,
      TAccountMoneyManagerPool extends string
        ? WritableAccount<TAccountMoneyManagerPool>
        : TAccountMoneyManagerPool,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type CreateMoneyManagerPoolInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type CreateMoneyManagerPoolInstructionDataArgs = {};

export function getCreateMoneyManagerPoolInstructionDataEncoder(): FixedSizeEncoder<CreateMoneyManagerPoolInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({
      ...value,
      discriminator: CREATE_MONEY_MANAGER_POOL_DISCRIMINATOR,
    }),
  );
}

export function getCreateMoneyManagerPoolInstructionDataDecoder(): FixedSizeDecoder<CreateMoneyManagerPoolInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getCreateMoneyManagerPoolInstructionDataCodec(): FixedSizeCodec<
  CreateMoneyManagerPoolInstructionDataArgs,
  CreateMoneyManagerPoolInstructionData
> {
  return combineCodec(
    getCreateMoneyManagerPoolInstructionDataEncoder(),
    getCreateMoneyManagerPoolInstructionDataDecoder(),
  );
}

export type CreateMoneyManagerPoolAsyncInput<
  TAccountAdminPool extends string = string,
  TAccountMoneyManager extends string = string,
  TAccountMoneyManagerPool extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  adminPool?: Address<TAccountAdminPool>;
  moneyManager: TransactionSigner<TAccountMoneyManager>;
  moneyManagerPool?: Address<TAccountMoneyManagerPool>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export async function getCreateMoneyManagerPoolInstructionAsync<
  TAccountAdminPool extends string,
  TAccountMoneyManager extends string,
  TAccountMoneyManagerPool extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof FBYT_VAULT_PROGRAM_ADDRESS,
>(
  input: CreateMoneyManagerPoolAsyncInput<
    TAccountAdminPool,
    TAccountMoneyManager,
    TAccountMoneyManagerPool,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  CreateMoneyManagerPoolInstruction<
    TProgramAddress,
    TAccountAdminPool,
    TAccountMoneyManager,
    TAccountMoneyManagerPool,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? FBYT_VAULT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    adminPool: { value: input.adminPool ?? null, isWritable: false },
    moneyManager: { value: input.moneyManager ?? null, isWritable: true },
    moneyManagerPool: {
      value: input.moneyManagerPool ?? null,
      isWritable: true,
    },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.adminPool.value) {
    accounts.adminPool.value = await findAdminPoolPda({ programAddress });
  }
  if (!accounts.moneyManagerPool.value) {
    accounts.moneyManagerPool.value = await findMoneyManagerPoolPda(
      {
        adminPool: getAddressFromResolvedInstructionAccount(
          "adminPool",
          accounts.adminPool.value,
        ),
        moneyManager: getAddressFromResolvedInstructionAccount(
          "moneyManager",
          accounts.moneyManager.value,
        ),
      },
      { programAddress },
    );
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("adminPool", accounts.adminPool),
      getAccountMeta("moneyManager", accounts.moneyManager),
      getAccountMeta("moneyManagerPool", accounts.moneyManagerPool),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getCreateMoneyManagerPoolInstructionDataEncoder().encode({}),
    programAddress,
  } as CreateMoneyManagerPoolInstruction<
    TProgramAddress,
    TAccountAdminPool,
    TAccountMoneyManager,
    TAccountMoneyManagerPool,
    TAccountSystemProgram
  >);
}

export type CreateMoneyManagerPoolInput<
  TAccountAdminPool extends string = string,
  TAccountMoneyManager extends string = string,
  TAccountMoneyManagerPool extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  adminPool: Address<TAccountAdminPool>;
  moneyManager: TransactionSigner<TAccountMoneyManager>;
  moneyManagerPool: Address<TAccountMoneyManagerPool>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export function getCreateMoneyManagerPoolInstruction<
  TAccountAdminPool extends string,
  TAccountMoneyManager extends string,
  TAccountMoneyManagerPool extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof FBYT_VAULT_PROGRAM_ADDRESS,
>(
  input: CreateMoneyManagerPoolInput<
    TAccountAdminPool,
    TAccountMoneyManager,
    TAccountMoneyManagerPool,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): CreateMoneyManagerPoolInstruction<
  TProgramAddress,
  TAccountAdminPool,
  TAccountMoneyManager,
  TAccountMoneyManagerPool,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? FBYT_VAULT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    adminPool: { value: input.adminPool ?? null, isWritable: false },
    moneyManager: { value: input.moneyManager ?? null, isWritable: true },
    moneyManagerPool: {
      value: input.moneyManagerPool ?? null,
      isWritable: true,
    },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("adminPool", accounts.adminPool),
      getAccountMeta("moneyManager", accounts.moneyManager),
      getAccountMeta("moneyManagerPool", accounts.moneyManagerPool),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getCreateMoneyManagerPoolInstructionDataEncoder().encode({}),
    programAddress,
  } as CreateMoneyManagerPoolInstruction<
    TProgramAddress,
    TAccountAdminPool,
    TAccountMoneyManager,
    TAccountMoneyManagerPool,
    TAccountSystemProgram
  >);
}

export type ParsedCreateMoneyManagerPoolInstruction<
  TProgram extends string = typeof FBYT_VAULT_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    adminPool: TAccountMetas[0];
    moneyManager: TAccountMetas[1];
    moneyManagerPool: TAccountMetas[2];
    systemProgram: TAccountMetas[3];
  };
  data: CreateMoneyManagerPoolInstructionData;
};

export function parseCreateMoneyManagerPoolInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedCreateMoneyManagerPoolInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 4,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      adminPool: getNextAccount(),
      moneyManager: getNextAccount(),
      moneyManagerPool: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getCreateMoneyManagerPoolInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
