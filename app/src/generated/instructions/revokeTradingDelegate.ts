/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { FBYT_VAULT_PROGRAM_ADDRESS } from "../programs";

export const REVOKE_TRADING_DELEGATE_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([43, 207, 174, 48, 174, 77, 188, 120]);

export function getRevokeTradingDelegateDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    REVOKE_TRADING_DELEGATE_DISCRIMINATOR,
  );
}

export type RevokeTradingDelegateInstruction<
  TProgram extends string = typeof FBYT_VAULT_PROGRAM_ADDRESS,
  TAccountVaultPool extends string | AccountMeta<string> = string,
  TAccountMoneyManager extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountVaultPool extends string
        ? WritableAccount<TAccountVaultPool>
        : TAccountVaultPool,
      TAccountMoneyManager extends string
        ? ReadonlySignerAccount<TAccountMoneyManager> &
            AccountSignerMeta<TAccountMoneyManager>
        : TAccountMoneyManager,
      ...TRemainingAccounts,
    ]
  >;

export type RevokeTradingDelegateInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type RevokeTradingDelegateInstructionDataArgs = {};

export function getRevokeTradingDelegateInstructionDataEncoder(): FixedSizeEncoder<RevokeTradingDelegateInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({
      ...value,
      discriminator: REVOKE_TRADING_DELEGATE_DISCRIMINATOR,
    }),
  );
}

export function getRevokeTradingDelegateInstructionDataDecoder(): FixedSizeDecoder<RevokeTradingDelegateInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getRevokeTradingDelegateInstructionDataCodec(): FixedSizeCodec<
  RevokeTradingDelegateInstructionDataArgs,
  RevokeTradingDelegateInstructionData
> {
  return combineCodec(
    getRevokeTradingDelegateInstructionDataEncoder(),
    getRevokeTradingDelegateInstructionDataDecoder(),
  );
}

export type RevokeTradingDelegateInput<
  TAccountVaultPool extends string = string,
  TAccountMoneyManager extends string = string,
> = {
  vaultPool: Address<TAccountVaultPool>;
  moneyManager: TransactionSigner<TAccountMoneyManager>;
};

export function getRevokeTradingDelegateInstruction<
  TAccountVaultPool extends string,
  TAccountMoneyManager extends string,
  TProgramAddress extends Address = typeof FBYT_VAULT_PROGRAM_ADDRESS,
>(
  input: RevokeTradingDelegateInput<TAccountVaultPool, TAccountMoneyManager>,
  config?: { programAddress?: TProgramAddress },
): RevokeTradingDelegateInstruction<
  TProgramAddress,
  TAccountVaultPool,
  TAccountMoneyManager
> {
  // Program address.
  const programAddress = config?.programAddress ?? FBYT_VAULT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    vaultPool: { value: input.vaultPool ?? null, isWritable: true },
    moneyManager: { value: input.moneyManager ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("vaultPool", accounts.vaultPool),
      getAccountMeta("moneyManager", accounts.moneyManager),
    ],
    data: getRevokeTradingDelegateInstructionDataEncoder().encode({}),
    programAddress,
  } as RevokeTradingDelegateInstruction<
    TProgramAddress,
    TAccountVaultPool,
    TAccountMoneyManager
  >);
}

export type ParsedRevokeTradingDelegateInstruction<
  TProgram extends string = typeof FBYT_VAULT_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    vaultPool: TAccountMetas[0];
    moneyManager: TAccountMetas[1];
  };
  data: RevokeTradingDelegateInstructionData;
};

export function parseRevokeTradingDelegateInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedRevokeTradingDelegateInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 2,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { vaultPool: getNextAccount(), moneyManager: getNextAccount() },
    data: getRevokeTradingDelegateInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
