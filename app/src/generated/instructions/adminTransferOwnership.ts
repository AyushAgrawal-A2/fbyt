/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findAdminPoolPda } from "../pdas";
import { FBYT_VAULT_PROGRAM_ADDRESS } from "../programs";

export const ADMIN_TRANSFER_OWNERSHIP_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([195, 184, 73, 211, 16, 243, 87, 87]);

export function getAdminTransferOwnershipDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    ADMIN_TRANSFER_OWNERSHIP_DISCRIMINATOR,
  );
}

export type AdminTransferOwnershipInstruction<
  TProgram extends string = typeof FBYT_VAULT_PROGRAM_ADDRESS,
  TAccountAdmin extends string | AccountMeta<string> = string,
  TAccountPendingAdmin extends string | AccountMeta<string> = string,
  TAccountAdminPool extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAdmin extends string
        ? WritableSignerAccount<TAccountAdmin> &
            AccountSignerMeta<TAccountAdmin>
        : TAccountAdmin,
      TAccountPendingAdmin extends string
        ? WritableAccount<TAccountPendingAdmin>
        : TAccountPendingAdmin,
      TAccountAdminPool extends string
        ? WritableAccount<TAccountAdminPool>
        : TAccountAdminPool,
      ...TRemainingAccounts,
    ]
  >;

export type AdminTransferOwnershipInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type AdminTransferOwnershipInstructionDataArgs = {};

export function getAdminTransferOwnershipInstructionDataEncoder(): FixedSizeEncoder<AdminTransferOwnershipInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({
      ...value,
      discriminator: ADMIN_TRANSFER_OWNERSHIP_DISCRIMINATOR,
    }),
  );
}

export function getAdminTransferOwnershipInstructionDataDecoder(): FixedSizeDecoder<AdminTransferOwnershipInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getAdminTransferOwnershipInstructionDataCodec(): FixedSizeCodec<
  AdminTransferOwnershipInstructionDataArgs,
  AdminTransferOwnershipInstructionData
> {
  return combineCodec(
    getAdminTransferOwnershipInstructionDataEncoder(),
    getAdminTransferOwnershipInstructionDataDecoder(),
  );
}

export type AdminTransferOwnershipAsyncInput<
  TAccountAdmin extends string = string,
  TAccountPendingAdmin extends string = string,
  TAccountAdminPool extends string = string,
> = {
  admin: TransactionSigner<TAccountAdmin>;
  /** nominated; it only takes effect once it signs `admin_accept_ownership`. */
  pendingAdmin: Address<TAccountPendingAdmin>;
  adminPool?: Address<TAccountAdminPool>;
};

export async function getAdminTransferOwnershipInstructionAsync<
  TAccountAdmin extends string,
  TAccountPendingAdmin extends string,
  TAccountAdminPool extends string,
  TProgramAddress extends Address = typeof FBYT_VAULT_PROGRAM_ADDRESS,
>(
  input: AdminTransferOwnershipAsyncInput<
    TAccountAdmin,
    TAccountPendingAdmin,
    TAccountAdminPool
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  AdminTransferOwnershipInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountPendingAdmin,
    TAccountAdminPool
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? FBYT_VAULT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    admin: { value: input.admin ?? null, isWritable: true },
    pendingAdmin: { value: input.pendingAdmin ?? null, isWritable: true },
    adminPool: { value: input.adminPool ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.adminPool.value) {
    accounts.adminPool.value = await findAdminPoolPda({ programAddress });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("admin", accounts.admin),
      getAccountMeta("pendingAdmin", accounts.pendingAdmin),
      getAccountMeta("adminPool", accounts.adminPool),
    ],
    data: getAdminTransferOwnershipInstructionDataEncoder().encode({}),
    programAddress,
  } as AdminTransferOwnershipInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountPendingAdmin,
    TAccountAdminPool
  >);
}

export type AdminTransferOwnershipInput<
  TAccountAdmin extends string = string,
  TAccountPendingAdmin extends string = string,
  TAccountAdminPool extends string = string,
> = {
  admin: TransactionSigner<TAccountAdmin>;
  /** nominated; it only takes effect once it signs `admin_accept_ownership`. */
  pendingAdmin: Address<TAccountPendingAdmin>;
  adminPool: Address<TAccountAdminPool>;
};

export function getAdminTransferOwnershipInstruction<
  TAccountAdmin extends string,
  TAccountPendingAdmin extends string,
  TAccountAdminPool extends string,
  TProgramAddress extends Address = typeof FBYT_VAULT_PROGRAM_ADDRESS,
>(
  input: AdminTransferOwnershipInput<
    TAccountAdmin,
    TAccountPendingAdmin,
    TAccountAdminPool
  >,
  config?: { programAddress?: TProgramAddress },
): AdminTransferOwnershipInstruction<
  TProgramAddress,
  TAccountAdmin,
  TAccountPendingAdmin,
  TAccountAdminPool
> {
  // Program address.
  const programAddress = config?.programAddress ?? FBYT_VAULT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    admin: { value: input.admin ?? null, isWritable: true },
    pendingAdmin: { value: input.pendingAdmin ?? null, isWritable: true },
    adminPool: { value: input.adminPool ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("admin", accounts.admin),
      getAccountMeta("pendingAdmin", accounts.pendingAdmin),
      getAccountMeta("adminPool", accounts.adminPool),
    ],
    data: getAdminTransferOwnershipInstructionDataEncoder().encode({}),
    programAddress,
  } as AdminTransferOwnershipInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountPendingAdmin,
    TAccountAdminPool
  >);
}

export type ParsedAdminTransferOwnershipInstruction<
  TProgram extends string = typeof FBYT_VAULT_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    admin: TAccountMetas[0];
    /** nominated; it only takes effect once it signs `admin_accept_ownership`. */
    pendingAdmin: TAccountMetas[1];
    adminPool: TAccountMetas[2];
  };
  data: AdminTransferOwnershipInstructionData;
};

export function parseAdminTransferOwnershipInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedAdminTransferOwnershipInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      admin: getNextAccount(),
      pendingAdmin: getNextAccount(),
      adminPool: getNextAccount(),
    },
    data: getAdminTransferOwnershipInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
