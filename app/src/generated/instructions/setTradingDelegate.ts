/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { FBYT_VAULT_PROGRAM_ADDRESS } from "../programs";

export const SET_TRADING_DELEGATE_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([181, 195, 120, 236, 254, 84, 61, 77]);

export function getSetTradingDelegateDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    SET_TRADING_DELEGATE_DISCRIMINATOR,
  );
}

export type SetTradingDelegateInstruction<
  TProgram extends string = typeof FBYT_VAULT_PROGRAM_ADDRESS,
  TAccountVaultPool extends string | AccountMeta<string> = string,
  TAccountMoneyManager extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountVaultPool extends string
        ? WritableAccount<TAccountVaultPool>
        : TAccountVaultPool,
      TAccountMoneyManager extends string
        ? ReadonlySignerAccount<TAccountMoneyManager> &
            AccountSignerMeta<TAccountMoneyManager>
        : TAccountMoneyManager,
      ...TRemainingAccounts,
    ]
  >;

export type SetTradingDelegateInstructionData = {
  discriminator: ReadonlyUint8Array;
  tradingDelegate: Address;
};

export type SetTradingDelegateInstructionDataArgs = {
  tradingDelegate: Address;
};

export function getSetTradingDelegateInstructionDataEncoder(): FixedSizeEncoder<SetTradingDelegateInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["tradingDelegate", getAddressEncoder()],
    ]),
    (value) => ({
      ...value,
      discriminator: SET_TRADING_DELEGATE_DISCRIMINATOR,
    }),
  );
}

export function getSetTradingDelegateInstructionDataDecoder(): FixedSizeDecoder<SetTradingDelegateInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["tradingDelegate", getAddressDecoder()],
  ]);
}

export function getSetTradingDelegateInstructionDataCodec(): FixedSizeCodec<
  SetTradingDelegateInstructionDataArgs,
  SetTradingDelegateInstructionData
> {
  return combineCodec(
    getSetTradingDelegateInstructionDataEncoder(),
    getSetTradingDelegateInstructionDataDecoder(),
  );
}

export type SetTradingDelegateInput<
  TAccountVaultPool extends string = string,
  TAccountMoneyManager extends string = string,
> = {
  vaultPool: Address<TAccountVaultPool>;
  moneyManager: TransactionSigner<TAccountMoneyManager>;
  tradingDelegate: SetTradingDelegateInstructionDataArgs["tradingDelegate"];
};

export function getSetTradingDelegateInstruction<
  TAccountVaultPool extends string,
  TAccountMoneyManager extends string,
  TProgramAddress extends Address = typeof FBYT_VAULT_PROGRAM_ADDRESS,
>(
  input: SetTradingDelegateInput<TAccountVaultPool, TAccountMoneyManager>,
  config?: { programAddress?: TProgramAddress },
): SetTradingDelegateInstruction<
  TProgramAddress,
  TAccountVaultPool,
  TAccountMoneyManager
> {
  // Program address.
  const programAddress = config?.programAddress ?? FBYT_VAULT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    vaultPool: { value: input.vaultPool ?? null, isWritable: true },
    moneyManager: { value: input.moneyManager ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("vaultPool", accounts.vaultPool),
      getAccountMeta("moneyManager", accounts.moneyManager),
    ],
    data: getSetTradingDelegateInstructionDataEncoder().encode(
      args as SetTradingDelegateInstructionDataArgs,
    ),
    programAddress,
  } as SetTradingDelegateInstruction<
    TProgramAddress,
    TAccountVaultPool,
    TAccountMoneyManager
  >);
}

export type ParsedSetTradingDelegateInstruction<
  TProgram extends string = typeof FBYT_VAULT_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    vaultPool: TAccountMetas[0];
    moneyManager: TAccountMetas[1];
  };
  data: SetTradingDelegateInstructionData;
};

export function parseSetTradingDelegateInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedSetTradingDelegateInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 2,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { vaultPool: getNextAccount(), moneyManager: getNextAccount() },
    data: getSetTradingDelegateInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
