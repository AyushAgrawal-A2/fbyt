/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findAdminPoolPda, findOraclePoolPda } from "../pdas";
import { FBYT_VAULT_PROGRAM_ADDRESS } from "../programs";

export const CREATE_ORACLE_POOL_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([53, 101, 159, 127, 234, 217, 156, 223]);

export function getCreateOraclePoolDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CREATE_ORACLE_POOL_DISCRIMINATOR,
  );
}

export type CreateOraclePoolInstruction<
  TProgram extends string = typeof FBYT_VAULT_PROGRAM_ADDRESS,
  TAccountRequester extends string | AccountMeta<string> = string,
  TAccountAdminPool extends string | AccountMeta<string> = string,
  TAccountTokenMint extends string | AccountMeta<string> = string,
  TAccountOraclePool extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountRequester extends string
        ? WritableSignerAccount<TAccountRequester> &
            AccountSignerMeta<TAccountRequester>
        : TAccountRequester,
      TAccountAdminPool extends string
        ? WritableAccount<TAccountAdminPool>
        : TAccountAdminPool,
      TAccountTokenMint extends string
        ? WritableAccount<TAccountTokenMint>
        : TAccountTokenMint,
      TAccountOraclePool extends string
        ? WritableAccount<TAccountOraclePool>
        : TAccountOraclePool,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type CreateOraclePoolInstructionData = {
  discriminator: ReadonlyUint8Array;
  feedId: string;
};

export type CreateOraclePoolInstructionDataArgs = { feedId: string };

export function getCreateOraclePoolInstructionDataEncoder(): Encoder<CreateOraclePoolInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["feedId", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
    ]),
    (value) => ({ ...value, discriminator: CREATE_ORACLE_POOL_DISCRIMINATOR }),
  );
}

export function getCreateOraclePoolInstructionDataDecoder(): Decoder<CreateOraclePoolInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["feedId", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
  ]);
}

export function getCreateOraclePoolInstructionDataCodec(): Codec<
  CreateOraclePoolInstructionDataArgs,
  CreateOraclePoolInstructionData
> {
  return combineCodec(
    getCreateOraclePoolInstructionDataEncoder(),
    getCreateOraclePoolInstructionDataDecoder(),
  );
}

export type CreateOraclePoolAsyncInput<
  TAccountRequester extends string = string,
  TAccountAdminPool extends string = string,
  TAccountTokenMint extends string = string,
  TAccountOraclePool extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  requester: TransactionSigner<TAccountRequester>;
  adminPool?: Address<TAccountAdminPool>;
  tokenMint: Address<TAccountTokenMint>;
  oraclePool?: Address<TAccountOraclePool>;
  systemProgram?: Address<TAccountSystemProgram>;
  feedId: CreateOraclePoolInstructionDataArgs["feedId"];
};

export async function getCreateOraclePoolInstructionAsync<
  TAccountRequester extends string,
  TAccountAdminPool extends string,
  TAccountTokenMint extends string,
  TAccountOraclePool extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof FBYT_VAULT_PROGRAM_ADDRESS,
>(
  input: CreateOraclePoolAsyncInput<
    TAccountRequester,
    TAccountAdminPool,
    TAccountTokenMint,
    TAccountOraclePool,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  CreateOraclePoolInstruction<
    TProgramAddress,
    TAccountRequester,
    TAccountAdminPool,
    TAccountTokenMint,
    TAccountOraclePool,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? FBYT_VAULT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    requester: { value: input.requester ?? null, isWritable: true },
    adminPool: { value: input.adminPool ?? null, isWritable: true },
    tokenMint: { value: input.tokenMint ?? null, isWritable: true },
    oraclePool: { value: input.oraclePool ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.adminPool.value) {
    accounts.adminPool.value = await findAdminPoolPda({ programAddress });
  }
  if (!accounts.oraclePool.value) {
    accounts.oraclePool.value = await findOraclePoolPda(
      {
        adminPool: getAddressFromResolvedInstructionAccount(
          "adminPool",
          accounts.adminPool.value,
        ),
        tokenMint: getAddressFromResolvedInstructionAccount(
          "tokenMint",
          accounts.tokenMint.value,
        ),
      },
      { programAddress },
    );
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("requester", accounts.requester),
      getAccountMeta("adminPool", accounts.adminPool),
      getAccountMeta("tokenMint", accounts.tokenMint),
      getAccountMeta("oraclePool", accounts.oraclePool),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getCreateOraclePoolInstructionDataEncoder().encode(
      args as CreateOraclePoolInstructionDataArgs,
    ),
    programAddress,
  } as CreateOraclePoolInstruction<
    TProgramAddress,
    TAccountRequester,
    TAccountAdminPool,
    TAccountTokenMint,
    TAccountOraclePool,
    TAccountSystemProgram
  >);
}

export type CreateOraclePoolInput<
  TAccountRequester extends string = string,
  TAccountAdminPool extends string = string,
  TAccountTokenMint extends string = string,
  TAccountOraclePool extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  requester: TransactionSigner<TAccountRequester>;
  adminPool: Address<TAccountAdminPool>;
  tokenMint: Address<TAccountTokenMint>;
  oraclePool: Address<TAccountOraclePool>;
  systemProgram?: Address<TAccountSystemProgram>;
  feedId: CreateOraclePoolInstructionDataArgs["feedId"];
};

export function getCreateOraclePoolInstruction<
  TAccountRequester extends string,
  TAccountAdminPool extends string,
  TAccountTokenMint extends string,
  TAccountOraclePool extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof FBYT_VAULT_PROGRAM_ADDRESS,
>(
  input: CreateOraclePoolInput<
    TAccountRequester,
    TAccountAdminPool,
    TAccountTokenMint,
    TAccountOraclePool,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): CreateOraclePoolInstruction<
  TProgramAddress,
  TAccountRequester,
  TAccountAdminPool,
  TAccountTokenMint,
  TAccountOraclePool,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? FBYT_VAULT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    requester: { value: input.requester ?? null, isWritable: true },
    adminPool: { value: input.adminPool ?? null, isWritable: true },
    tokenMint: { value: input.tokenMint ?? null, isWritable: true },
    oraclePool: { value: input.oraclePool ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("requester", accounts.requester),
      getAccountMeta("adminPool", accounts.adminPool),
      getAccountMeta("tokenMint", accounts.tokenMint),
      getAccountMeta("oraclePool", accounts.oraclePool),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getCreateOraclePoolInstructionDataEncoder().encode(
      args as CreateOraclePoolInstructionDataArgs,
    ),
    programAddress,
  } as CreateOraclePoolInstruction<
    TProgramAddress,
    TAccountRequester,
    TAccountAdminPool,
    TAccountTokenMint,
    TAccountOraclePool,
    TAccountSystemProgram
  >);
}

export type ParsedCreateOraclePoolInstruction<
  TProgram extends string = typeof FBYT_VAULT_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    requester: TAccountMetas[0];
    adminPool: TAccountMetas[1];
    tokenMint: TAccountMetas[2];
    oraclePool: TAccountMetas[3];
    systemProgram: TAccountMetas[4];
  };
  data: CreateOraclePoolInstructionData;
};

export function parseCreateOraclePoolInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedCreateOraclePoolInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 5,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      requester: getNextAccount(),
      adminPool: getNextAccount(),
      tokenMint: getNextAccount(),
      oraclePool: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getCreateOraclePoolInstructionDataDecoder().decode(instruction.data),
  };
}
