/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU16Decoder,
  getU16Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findAdminPoolPda } from "../pdas";
import { FBYT_VAULT_PROGRAM_ADDRESS } from "../programs";

export const ADMIN_UPDATE_MAX_SLIPPAGE_BPS_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([214, 191, 45, 126, 108, 57, 132, 209]);

export function getAdminUpdateMaxSlippageBpsDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    ADMIN_UPDATE_MAX_SLIPPAGE_BPS_DISCRIMINATOR,
  );
}

export type AdminUpdateMaxSlippageBpsInstruction<
  TProgram extends string = typeof FBYT_VAULT_PROGRAM_ADDRESS,
  TAccountAdmin extends string | AccountMeta<string> = string,
  TAccountAdminPool extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAdmin extends string
        ? WritableSignerAccount<TAccountAdmin> &
            AccountSignerMeta<TAccountAdmin>
        : TAccountAdmin,
      TAccountAdminPool extends string
        ? WritableAccount<TAccountAdminPool>
        : TAccountAdminPool,
      ...TRemainingAccounts,
    ]
  >;

export type AdminUpdateMaxSlippageBpsInstructionData = {
  discriminator: ReadonlyUint8Array;
  newMaxSlippageBps: number;
};

export type AdminUpdateMaxSlippageBpsInstructionDataArgs = {
  newMaxSlippageBps: number;
};

export function getAdminUpdateMaxSlippageBpsInstructionDataEncoder(): FixedSizeEncoder<AdminUpdateMaxSlippageBpsInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["newMaxSlippageBps", getU16Encoder()],
    ]),
    (value) => ({
      ...value,
      discriminator: ADMIN_UPDATE_MAX_SLIPPAGE_BPS_DISCRIMINATOR,
    }),
  );
}

export function getAdminUpdateMaxSlippageBpsInstructionDataDecoder(): FixedSizeDecoder<AdminUpdateMaxSlippageBpsInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["newMaxSlippageBps", getU16Decoder()],
  ]);
}

export function getAdminUpdateMaxSlippageBpsInstructionDataCodec(): FixedSizeCodec<
  AdminUpdateMaxSlippageBpsInstructionDataArgs,
  AdminUpdateMaxSlippageBpsInstructionData
> {
  return combineCodec(
    getAdminUpdateMaxSlippageBpsInstructionDataEncoder(),
    getAdminUpdateMaxSlippageBpsInstructionDataDecoder(),
  );
}

export type AdminUpdateMaxSlippageBpsAsyncInput<
  TAccountAdmin extends string = string,
  TAccountAdminPool extends string = string,
> = {
  admin: TransactionSigner<TAccountAdmin>;
  adminPool?: Address<TAccountAdminPool>;
  newMaxSlippageBps: AdminUpdateMaxSlippageBpsInstructionDataArgs["newMaxSlippageBps"];
};

export async function getAdminUpdateMaxSlippageBpsInstructionAsync<
  TAccountAdmin extends string,
  TAccountAdminPool extends string,
  TProgramAddress extends Address = typeof FBYT_VAULT_PROGRAM_ADDRESS,
>(
  input: AdminUpdateMaxSlippageBpsAsyncInput<TAccountAdmin, TAccountAdminPool>,
  config?: { programAddress?: TProgramAddress },
): Promise<
  AdminUpdateMaxSlippageBpsInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountAdminPool
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? FBYT_VAULT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    admin: { value: input.admin ?? null, isWritable: true },
    adminPool: { value: input.adminPool ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.adminPool.value) {
    accounts.adminPool.value = await findAdminPoolPda({ programAddress });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("admin", accounts.admin),
      getAccountMeta("adminPool", accounts.adminPool),
    ],
    data: getAdminUpdateMaxSlippageBpsInstructionDataEncoder().encode(
      args as AdminUpdateMaxSlippageBpsInstructionDataArgs,
    ),
    programAddress,
  } as AdminUpdateMaxSlippageBpsInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountAdminPool
  >);
}

export type AdminUpdateMaxSlippageBpsInput<
  TAccountAdmin extends string = string,
  TAccountAdminPool extends string = string,
> = {
  admin: TransactionSigner<TAccountAdmin>;
  adminPool: Address<TAccountAdminPool>;
  newMaxSlippageBps: AdminUpdateMaxSlippageBpsInstructionDataArgs["newMaxSlippageBps"];
};

export function getAdminUpdateMaxSlippageBpsInstruction<
  TAccountAdmin extends string,
  TAccountAdminPool extends string,
  TProgramAddress extends Address = typeof FBYT_VAULT_PROGRAM_ADDRESS,
>(
  input: AdminUpdateMaxSlippageBpsInput<TAccountAdmin, TAccountAdminPool>,
  config?: { programAddress?: TProgramAddress },
): AdminUpdateMaxSlippageBpsInstruction<
  TProgramAddress,
  TAccountAdmin,
  TAccountAdminPool
> {
  // Program address.
  const programAddress = config?.programAddress ?? FBYT_VAULT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    admin: { value: input.admin ?? null, isWritable: true },
    adminPool: { value: input.adminPool ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("admin", accounts.admin),
      getAccountMeta("adminPool", accounts.adminPool),
    ],
    data: getAdminUpdateMaxSlippageBpsInstructionDataEncoder().encode(
      args as AdminUpdateMaxSlippageBpsInstructionDataArgs,
    ),
    programAddress,
  } as AdminUpdateMaxSlippageBpsInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountAdminPool
  >);
}

export type ParsedAdminUpdateMaxSlippageBpsInstruction<
  TProgram extends string = typeof FBYT_VAULT_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    admin: TAccountMetas[0];
    adminPool: TAccountMetas[1];
  };
  data: AdminUpdateMaxSlippageBpsInstructionData;
};

export function parseAdminUpdateMaxSlippageBpsInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedAdminUpdateMaxSlippageBpsInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 2,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { admin: getNextAccount(), adminPool: getNextAccount() },
    data: getAdminUpdateMaxSlippageBpsInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
